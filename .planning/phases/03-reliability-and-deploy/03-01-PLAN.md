---
phase: 03-reliability-and-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/sync/route.ts
  - src/components/SyncDashboard.tsx
  - src/app/api/sync/__tests__/classifyError.test.ts
  - src/components/__tests__/SyncDashboard.test.tsx
  - src/components/__tests__/SyncSummary.test.tsx
autonomous: true
requirements: [SYNC-02, SYNC-03, SYNC-04]

must_haves:
  truths:
    - "After sync completes, user sees a 'Last synced' timestamp below the sync button"
    - "Timestamp persists across page reload (localStorage)"
    - "After sync completes, user sees summary counts (created, updated, unchanged, failed)"
    - "When auth token is expired, user sees 'reconnect your account' message, not a generic error"
    - "When Google Calendar quota is exceeded, user sees 'wait a few minutes' message"
    - "When Canvas ICS feed is unreachable, user sees 'check ICS URL' message"
    - "Background sync job completes on Vercel (after() instead of void promise)"
  artifacts:
    - path: "src/app/api/sync/route.ts"
      provides: "classifyError function + after() lifecycle"
      contains: "classifyError"
    - path: "src/components/SyncDashboard.tsx"
      provides: "localStorage timestamp persistence + display"
      contains: "lastSyncedAt"
    - path: "src/app/api/sync/__tests__/classifyError.test.ts"
      provides: "Unit tests for error classification"
      contains: "classifyError"
    - path: "src/components/__tests__/SyncDashboard.test.tsx"
      provides: "Unit tests for timestamp persistence"
      contains: "localStorage"
    - path: "src/components/__tests__/SyncSummary.test.tsx"
      provides: "Unit tests for sync summary display"
      contains: "SyncSummary"
  key_links:
    - from: "src/app/api/sync/route.ts"
      to: "SyncJobState.error"
      via: "classifyError in catch block"
      pattern: "classifyError"
    - from: "src/components/SyncDashboard.tsx"
      to: "localStorage"
      via: "useEffect read on mount, write on poll complete"
      pattern: "localStorage.*lastSyncedAt"
---

<objective>
Wire sync feedback (timestamp, error classification) and fix Vercel lifecycle.

Purpose: SYNC-02 (last synced timestamp), SYNC-03 (summary display already built -- add test coverage), SYNC-04 (actionable error messages instead of generic "Sync failed"). Also replace fire-and-forget `void` promise with `after()` from `next/server` so background sync completes on Vercel.

Output: Updated SyncDashboard.tsx with localStorage timestamp, updated sync route.ts with classifyError + after(), unit tests for all three concerns.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-reliability-and-deploy/03-RESEARCH.md
@.planning/phases/03-reliability-and-deploy/03-VALIDATION.md

@src/app/api/sync/route.ts
@src/app/api/sync/status/route.ts
@src/components/SyncDashboard.tsx
@src/components/SyncSummary.tsx
@src/components/SyncButton.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/api/sync/route.ts:
```typescript
export interface SyncJobState {
  status: 'running' | 'complete' | 'error';
  progress: SyncProgress[];
  canvasSummary?: SyncSummary;
  mirrorSummary?: MirrorSummary;
  error?: string;
  completedAt?: number;
}
export const syncJobs = new Map<string, SyncJobState>();
```

From src/components/SyncDashboard.tsx:
```typescript
interface SyncDashboardProps {
  userName: string;
  hasCanvasUrl: boolean;
  hasSchoolAccount: boolean;
}
type SyncStatus = 'idle' | 'running' | 'complete' | 'error';
```

From src/components/SyncSummary.tsx:
```typescript
interface SyncJobSummary {
  inserted: number;
  updated: number;
  skipped: number;
  failed: number;
  errors: string[];
}
interface SyncSummaryProps {
  canvasSummary?: SyncJobSummary;
  mirrorSummary?: SyncJobSummary;
  onDismiss: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add classifyError to sync route + replace void with after()</name>
  <files>src/app/api/sync/route.ts, src/app/api/sync/__tests__/classifyError.test.ts</files>
  <behavior>
    - Test: classifyError with "Invalid Credentials" returns message containing "reconnect"
    - Test: classifyError with "invalid_grant" returns message containing "reconnect"
    - Test: classifyError with "Rate Limit Exceeded" returns message containing "quota" and "wait"
    - Test: classifyError with "quotaExceeded" returns message containing "quota"
    - Test: classifyError with "Calendar usage limits exceeded" returns message containing "quota"
    - Test: classifyError with error containing "fetch" or "canvas" returns message containing "Canvas feed" and "ICS URL"
    - Test: classifyError with unknown error returns "Sync failed: {message}"
    - Test: classifyError with non-Error (string) returns "Sync failed: {string}"
  </behavior>
  <action>
1. Create `src/app/api/sync/__tests__/classifyError.test.ts`:
   - Export `classifyError` from route.ts so tests can import it
   - Write tests for each error classification case per the behavior block above

2. In `src/app/api/sync/route.ts`:
   - Add and export `classifyError(err: unknown): string` function:
     - Check `err instanceof Error ? err.message : String(err)`
     - lowercase the message for matching
     - If contains "invalid credentials" or "invalid_grant" or "no access token" -> "Your Google account connection has expired. Go to Settings and reconnect your account."
     - If contains "rate limit" or "quota" or "usagelimits" -> "Google Calendar quota exceeded. Please wait a few minutes and try again."
     - If contains "canvas" or "ics" or "fetch" (case insensitive) -> "Could not fetch your Canvas feed. Check that the ICS URL is still valid in Settings."
     - Default: `Sync failed: ${msg}`
   - Replace `job.error = err instanceof Error ? err.message : 'Sync failed'` in the catch block with `job.error = classifyError(err)`
   - Replace `void runSyncJob(jobId, userId, user.canvasIcsUrl)` with:
     ```typescript
     import { after } from 'next/server';
     // ...
     after(runSyncJob(jobId, userId, user.canvasIcsUrl));
     ```
     If `after` is not available in this Next.js version (import fails), fall back to `void` with a `// TODO: switch to after() when available` comment. Test the import with `npm run build` or by checking that the dev server doesn't error.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="classifyError" --no-coverage</automated>
  </verify>
  <done>classifyError is exported and tested (8 cases), catch block uses it, fire-and-forget replaced with after() or documented fallback. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add localStorage timestamp to SyncDashboard + test coverage</name>
  <files>src/components/SyncDashboard.tsx, src/components/__tests__/SyncDashboard.test.tsx</files>
  <behavior>
    - Test: On mount with no localStorage value, no "Last synced" text is rendered
    - Test: On mount with localStorage "lastSyncedAt" = "1710000000000", renders "Last synced" text with a formatted date
    - Test: After sync completes (mock poll returns status: 'complete'), localStorage "lastSyncedAt" is set to a recent epoch ms value
  </behavior>
  <action>
1. Create `src/components/__tests__/` directory if it doesn't exist.

2. Create `src/components/__tests__/SyncDashboard.test.tsx`:
   - Use jest with jsdom environment (add `@jest-environment jsdom` docblock)
   - Mock `fetch` globally to return appropriate responses for /api/parse-ics, /api/school-calendars, /api/sync, /api/sync/status
   - Mock localStorage using `jest.spyOn(Storage.prototype, 'getItem')` and `jest.spyOn(Storage.prototype, 'setItem')`
   - Test the 3 behaviors listed above
   - For the "sync completes" test: mock fetch for POST /api/sync to return `{ jobId: 'test-id' }`, then mock GET /api/sync/status to return `{ status: 'complete', progress: [], canvasSummary: { inserted: 1, updated: 0, skipped: 0, failed: 0, errors: [] } }`. Verify localStorage.setItem was called with 'lastSyncedAt' and a numeric string.
   - Note: SyncDashboard imports CourseAccordion, SchoolCalendarList, SyncButton, SyncSummary. Use `jest.mock()` to mock these child components as simple div stubs to keep the test focused.

3. In `src/components/SyncDashboard.tsx`:
   - Add state: `const [lastSyncedAt, setLastSyncedAt] = useState<number | null>(null);`
   - Add useEffect to read from localStorage on mount:
     ```typescript
     useEffect(() => {
       const stored = localStorage.getItem('lastSyncedAt');
       if (stored) setLastSyncedAt(Number(stored));
     }, []);
     ```
   - In the poll-complete handler (inside the `statusData.status === 'complete'` block, after `setSyncStatus('complete')`):
     ```typescript
     const now = Date.now();
     localStorage.setItem('lastSyncedAt', String(now));
     setLastSyncedAt(now);
     ```
   - Render the timestamp in the header area, below the subtitle paragraph:
     ```tsx
     {lastSyncedAt !== null && (
       <p className="text-xs text-[--color-text-secondary]">
         Last synced {new Date(lastSyncedAt).toLocaleString()}
       </p>
     )}
     ```
  </action>
  <verify>
    <automated>npx jest --testPathPattern="SyncDashboard" --no-coverage</automated>
  </verify>
  <done>SyncDashboard shows "Last synced" timestamp after sync completes. Timestamp survives page reload via localStorage. Tests pass for mount-with-no-value, mount-with-stored-value, and write-on-complete scenarios. Full suite still green: `npx jest --no-coverage`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add SyncSummary test coverage (SYNC-03)</name>
  <files>src/components/__tests__/SyncSummary.test.tsx</files>
  <behavior>
    - Test: renders nothing when both canvasSummary and mirrorSummary are undefined
    - Test: renders canvas summary counts (inserted, updated, skipped) when canvasSummary is provided
    - Test: renders mirror summary counts when mirrorSummary is provided
    - Test: renders both canvas and mirror summaries when both are provided
    - Test: calls onDismiss when dismiss button is clicked
    - Test: shows failed count only when failed > 0
  </behavior>
  <action>
1. Create `src/components/__tests__/SyncSummary.test.tsx`:
   - Use jest with jsdom environment (add `@jest-environment jsdom` docblock)
   - Import `render, screen, fireEvent` from `@testing-library/react`
   - Import SyncSummary from `../SyncSummary`
   - Write tests for each behavior:
     - "renders nothing when no summaries": render with canvasSummary=undefined, mirrorSummary=undefined. Assert `container.firstChild` is null.
     - "renders canvas summary counts": render with canvasSummary={ inserted: 3, updated: 1, skipped: 5, failed: 0, errors: [] }. Assert screen contains "3 created", "1 updated", "5 unchanged". Assert "failed" is NOT in the document.
     - "renders mirror summary counts": render with mirrorSummary={ inserted: 2, updated: 0, skipped: 3, failed: 1, errors: ['some error'] }. Assert screen contains "2 created", "0 updated", "3 unchanged", "1 failed".
     - "renders both summaries": provide both props, assert "Canvas" and "School Mirror" labels both appear.
     - "calls onDismiss": render with canvasSummary and onDismiss=jest.fn(). Click the dismiss button (aria-label="Dismiss"). Assert onDismiss was called once.
     - "shows failed count only when > 0": render with failed=0, assert no "failed" text. Render with failed=2, assert "2 failed" appears.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="SyncSummary" --no-coverage</automated>
  </verify>
  <done>SyncSummary component has full test coverage: renders correct counts for canvas and mirror summaries, dismiss button fires callback, failed count conditionally shown. SYNC-03 requirement is verified by automated tests.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `npx jest --no-coverage` — full test suite green
2. `npm run build` — no build errors (validates after() import if used)
3. Manual: run dev server, trigger sync, verify "Last synced" appears and persists on reload
4. Manual: the SyncSummary still renders with created/updated/unchanged/failed counts (SYNC-03 unchanged)
</verification>

<success_criteria>
- classifyError maps auth/quota/canvas errors to actionable user-facing strings (SYNC-04)
- Last synced timestamp displays after sync and persists via localStorage (SYNC-02)
- Sync summary renders counts correctly with full test coverage (SYNC-03)
- Background sync uses after() for Vercel lifecycle safety
- All unit tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-reliability-and-deploy/03-01-SUMMARY.md`
</output>
