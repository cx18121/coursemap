---
phase: 01-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/index.ts
  - src/lib/tokens.ts
  - src/lib/session.ts
  - src/lib/__tests__/session.test.ts
  - src/lib/__tests__/tokens.test.ts
  - src/lib/db/__tests__/schema.test.ts
  - drizzle.config.ts
  - .env.example
  - package.json
autonomous: true
requirements: [AUTH-01, AUTH-03]
user_setup:
  - service: neon-postgres
    why: "Database for token and user storage"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console (https://console.neon.tech) -> Project -> Connection Details -> Connection string"
    dashboard_config:
      - task: "Create a Neon project if one doesn't exist"
        location: "Neon Console -> New Project"

must_haves:
  truths:
    - "Drizzle schema defines users, oauth_tokens tables with correct columns and constraints"
    - "Token encryption round-trips correctly (encrypt then decrypt returns original)"
    - "Session encrypt/decrypt round-trips correctly (JWT contains userId)"
    - "DB client connects to Neon via HTTP driver"
    - "Schema exports are valid and constraint structure is correct (verified by test)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "users and oauth_tokens table definitions"
      contains: "pgTable"
    - path: "src/lib/db/index.ts"
      provides: "Drizzle client instance"
      exports: ["db"]
    - path: "src/lib/tokens.ts"
      provides: "AES-256-GCM token encryption/decryption"
      exports: ["encryptToken", "decryptToken"]
    - path: "src/lib/session.ts"
      provides: "jose-based session JWT encrypt/decrypt and cookie management"
      exports: ["encryptSession", "decryptSession", "setSessionCookie", "getSession", "clearSessionCookie"]
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit migration configuration"
    - path: "src/lib/__tests__/session.test.ts"
      provides: "Unit tests for session module"
    - path: "src/lib/__tests__/tokens.test.ts"
      provides: "Unit tests for token encryption module"
    - path: "src/lib/db/__tests__/schema.test.ts"
      provides: "Unit tests for schema exports and constraint structure"
  key_links:
    - from: "src/lib/db/index.ts"
      to: "src/lib/db/schema.ts"
      via: "schema import"
      pattern: "import.*schema"
    - from: "src/lib/tokens.ts"
      to: "process.env.TOKEN_ENCRYPTION_KEY"
      via: "env var for AES key"
      pattern: "TOKEN_ENCRYPTION_KEY"
    - from: "src/lib/session.ts"
      to: "process.env.SESSION_SECRET"
      via: "env var for JWT signing"
      pattern: "SESSION_SECRET"
---

<objective>
Install dependencies, create the database schema, token encryption utilities, and session management — the foundation layer that all OAuth routes and UI depend on.

Purpose: Every other plan in this phase imports from these lib modules. Building and testing them first ensures a stable contract.
Output: Tested lib/ modules (db, tokens, session), Drizzle config, env template, migrations.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create lib foundation (DB schema, tokens, session)</name>
  <files>package.json, src/lib/db/schema.ts, src/lib/db/index.ts, src/lib/tokens.ts, src/lib/session.ts, drizzle.config.ts, .env.example, src/lib/__tests__/session.test.ts, src/lib/__tests__/tokens.test.ts, src/lib/db/__tests__/schema.test.ts</files>
  <behavior>
    - encryptToken("hello") then decryptToken(result) returns "hello"
    - encryptToken produces format "base64:base64:base64" (iv:tag:ciphertext)
    - decryptToken with tampered ciphertext throws an error
    - encryptSession({ userId: 42 }) then decryptSession(result) returns object with userId === 42
    - decryptSession with garbage string returns null (does not throw)
    - decryptSession with expired token returns null
    - schema.ts exports `users` and `oauthTokens` table objects
    - oauthTokens has a unique constraint on (userId, role)
    - oauthTokens has a nullable `email` text column
  </behavior>
  <action>
    1. Install production deps: `npm install arctic jose @neondatabase/serverless drizzle-orm`
    2. Install dev dep: `npm install -D drizzle-kit`
    3. Create `src/lib/db/schema.ts` with Drizzle pgTable definitions:
       - `users` table: id (serial PK), googleId (text, unique, not null), email (text, not null), name (text, not null), canvasIcsUrl (text, nullable), createdAt (timestamp with tz, default now)
       - `oauthTokens` table: id (serial PK), userId (integer, references users.id with cascade delete, not null), role (text enum ["personal","school"], not null), email (text, nullable — stores the Google email for this account, used by AccountDropdown to show "email + role label"), encryptedAccessToken (text, not null), encryptedRefreshToken (text, nullable), accessTokenExpiresAt (timestamp with tz, not null), updatedAt (timestamp with tz, default now)
       - Add a unique index on (userId, role) for oauthTokens using `.unique()` on a composite or via `uniqueIndex`
    4. Create `src/lib/db/index.ts`: Import `drizzle` from `drizzle-orm/neon-http`, import schema, export `db` instance using `process.env.DATABASE_URL!`
    5. Create `src/lib/tokens.ts` with `encryptToken(plaintext: string): string` and `decryptToken(stored: string): string` using AES-256-GCM per Pattern 4 in RESEARCH.md. Key from `process.env.TOKEN_ENCRYPTION_KEY` (base64-encoded 32 bytes).
    6. Create `src/lib/session.ts` with `encryptSession`, `decryptSession`, `setSessionCookie`, `getSession`, `clearSessionCookie` per Pattern 3 in RESEARCH.md. TTL = 30 days. Cookie name = "session". Use jose SignJWT/jwtVerify with HS256. Secret from `process.env.SESSION_SECRET`.
    7. Create `drizzle.config.ts` per RESEARCH.md Code Examples section.
    8. Create `.env.example` with placeholder entries: DATABASE_URL, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SESSION_SECRET, TOKEN_ENCRYPTION_KEY, NEXT_PUBLIC_BASE_URL.
    9. Write tests FIRST (TDD red phase), then implement:
       - `src/lib/__tests__/tokens.test.ts`: Set TOKEN_ENCRYPTION_KEY env var to a generated 32-byte base64 key in beforeAll. Test encrypt/decrypt round-trip, format check (3 colon-separated base64 segments), tampered data throws.
       - `src/lib/__tests__/session.test.ts`: Set SESSION_SECRET env var in beforeAll. Test encrypt/decrypt round-trip returns userId, garbage input returns null, expired token returns null (mock time or use very short TTL).
       - `src/lib/db/__tests__/schema.test.ts`: Test that schema.ts exports `users` and `oauthTokens` as valid table objects. Verify oauthTokens has columns: userId, role, email, encryptedAccessToken, encryptedRefreshToken, accessTokenExpiresAt. Verify the unique constraint structure on (userId, role) exists. These are structural/export tests — no DB connection needed.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/coursemap && npx jest --testPathPattern="(session|tokens|schema)" --passWithNoTests</automated>
  </verify>
  <done>All tests pass. schema.ts exports users and oauthTokens tables (oauthTokens includes email column). tokens.ts exports encryptToken/decryptToken. session.ts exports all 5 session functions. drizzle.config.ts exists. .env.example lists all required env vars. Schema test verifies exports and constraint structure.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply Drizzle migration</name>
  <files>drizzle/</files>
  <action>
    1. Run `npx drizzle-kit generate` to produce SQL migration files from the schema
    2. Verify the generated SQL contains CREATE TABLE for users and oauth_tokens with correct columns (including the email column on oauth_tokens)
    3. Verify the unique constraint on (user_id, role) is present in the migration SQL
    4. Note: Do NOT run `drizzle-kit migrate` yet — that requires a live DATABASE_URL. The migration files are the artifact. The executor or user will apply them when Neon is configured.
  </action>
  <verify>
    <automated>ls /mnt/c/Users/charl/School/cs_misc/coursemap/drizzle/*.sql 2>/dev/null | head -5 && echo "Migration files exist"</automated>
  </verify>
  <done>drizzle/ directory contains at least one .sql migration file with CREATE TABLE statements for users and oauth_tokens.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="(session|tokens|schema)"` passes with all tests green
- `drizzle/` directory contains generated migration SQL
- `src/lib/db/schema.ts`, `src/lib/db/index.ts`, `src/lib/tokens.ts`, `src/lib/session.ts` all exist with correct exports
- `npm run build` does not error on the new lib files (may warn about unused exports, which is fine)
</verification>

<success_criteria>
- Token encryption round-trips correctly in tests
- Session JWT round-trips correctly in tests
- Drizzle schema defines both tables with all columns per RESEARCH.md Pattern 5, plus email on oauthTokens
- Schema test verifies exports and unique constraint structure
- Migration SQL is generated and ready to apply
- All new env vars documented in .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-01-SUMMARY.md`
</output>
