---
phase: 01-auth-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/SetupWizard.tsx
  - src/components/AccountDropdown.tsx
  - src/components/ReconnectBanner.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/dashboard/page.tsx
  - src/app/api/auth/me/route.ts
  - src/app/api/auth/canvas-url/route.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Unauthenticated user sees setup wizard with Step 1 (connect personal Google) active"
    - "After personal Google is connected, wizard advances to Step 2 (link school Google) with Skip option"
    - "After Step 2 (or skip), wizard advances to Step 3 (paste Canvas ICS URL)"
    - "Wizard shows progress bar indicating current step (1 of 3, 2 of 3, 3 of 3)"
    - "Returning user with valid session and completed setup goes straight to dashboard"
    - "Account dropdown in nav bar shows connected accounts with email + role label"
    - "Account dropdown shows 'Link school account' option when school is not connected"
    - "Single 'Sign out' button in dropdown clears everything and returns to wizard"
    - "Token refresh failure shows non-blocking reconnect banner at top of page"
    - "School OAuth blocked shows clear message with 'Try again' and 'Skip for now' buttons"
    - "/api/auth/me returns reconnectNeeded flags when getFreshAccessToken returns null"
    - "layout.tsx conditionally renders ReconnectBanner based on /api/auth/me reconnectNeeded data"
  artifacts:
    - path: "src/components/SetupWizard.tsx"
      provides: "3-step setup wizard with progress bar and OAuth buttons"
      min_lines: 80
    - path: "src/components/AccountDropdown.tsx"
      provides: "Nav bar dropdown showing connected accounts and actions"
      min_lines: 40
    - path: "src/components/ReconnectBanner.tsx"
      provides: "Non-blocking banner for token refresh failures"
      min_lines: 20
    - path: "src/app/page.tsx"
      provides: "Auth-aware root page routing (wizard vs redirect to dashboard)"
    - path: "src/app/dashboard/page.tsx"
      provides: "Protected dashboard page for returning users"
    - path: "src/app/layout.tsx"
      provides: "Root layout with nav bar containing AccountDropdown and ReconnectBanner"
    - path: "src/app/api/auth/me/route.ts"
      provides: "Session + account status API for client components, including reconnectNeeded flags"
      exports: ["GET"]
    - path: "src/app/api/auth/canvas-url/route.ts"
      provides: "Canvas ICS URL save endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/components/SetupWizard.tsx"
      to: "/login/google"
      via: "anchor href for personal Google sign-in"
      pattern: "/login/google"
    - from: "src/components/SetupWizard.tsx"
      to: "/link/school-google"
      via: "anchor href for school Google linking"
      pattern: "/link/school-google"
    - from: "src/components/AccountDropdown.tsx"
      to: "/api/auth/signout"
      via: "fetch POST on sign out click"
      pattern: "fetch.*signout"
    - from: "src/components/AccountDropdown.tsx"
      to: "/api/auth/me"
      via: "fetch GET for account status"
      pattern: "fetch.*auth/me"
    - from: "src/app/page.tsx"
      to: "src/lib/session.ts"
      via: "getSession to determine auth state"
      pattern: "getSession"
    - from: "src/app/layout.tsx"
      to: "src/components/ReconnectBanner.tsx"
      via: "conditionally renders ReconnectBanner when /api/auth/me indicates reconnectNeeded"
      pattern: "ReconnectBanner"
    - from: "src/app/api/auth/me/route.ts"
      to: "src/lib/tokens.ts"
      via: "calls getFreshAccessToken to detect reconnect-needed state"
      pattern: "getFreshAccessToken"
---

<objective>
Build the user-facing UI: setup wizard (3 steps with progress bar), account dropdown in nav bar, reconnect banner, auth-aware routing, and supporting API endpoints. This completes the auth foundation by giving users a way to interact with the OAuth flows built in Plan 02.

Purpose: Without UI, the OAuth routes are usable only via direct URL. This plan delivers the actual user experience: wizard for onboarding, dashboard for returning users, and account management via dropdown.
Output: 3 components, 2 API routes, updated root page + layout + dashboard page.
</objective>

<execution_context>
@/home/cx3429/.claude/get-shit-done/workflows/execute-plan.md
@/home/cx3429/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-foundation/01-CONTEXT.md
@.planning/phases/01-auth-foundation/01-RESEARCH.md
@.planning/phases/01-auth-foundation/01-01-SUMMARY.md
@.planning/phases/01-auth-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 + 02 outputs — executor needs these contracts -->

From src/lib/session.ts:
```typescript
export function getSession(): Promise<{ userId: number; exp: number } | null>;
export function clearSessionCookie(): Promise<void>;
```

From src/lib/db/schema.ts:
```typescript
export const users = pgTable("users", {
  id: serial("id").primaryKey(),
  googleId: text("google_id").notNull().unique(),
  email: text("email").notNull(),
  name: text("name").notNull(),
  canvasIcsUrl: text("canvas_ics_url"),
  createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull().defaultNow(),
});

export const oauthTokens = pgTable("oauth_tokens", {
  id: serial("id").primaryKey(),
  userId: integer("user_id").notNull().references(() => users.id, { onDelete: "cascade" }),
  role: text("role", { enum: ["personal", "school"] }).notNull(),
  email: text("email"),  // nullable — stores Google email for this account
  encryptedAccessToken: text("encrypted_access_token").notNull(),
  encryptedRefreshToken: text("encrypted_refresh_token"),
  accessTokenExpiresAt: timestamp("access_token_expires_at", { withTimezone: true, mode: "date" }).notNull(),
  updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull().defaultNow(),
});
```

From src/lib/db/index.ts:
```typescript
export const db: DrizzleClient;
```

From src/lib/tokens.ts:
```typescript
export function getFreshAccessToken(userId: number, role: "personal" | "school"): Promise<string | null>;
```

Existing route endpoints (from Plan 02):
- GET /login/google — initiates personal OAuth
- GET /link/school-google — initiates school OAuth (requires session)
- POST /api/auth/signout — clears session + tokens

Existing UI patterns (from src/components/CalendarSetup.tsx):
- Glassmorphic cards: `bg-white/10 p-6 rounded-2xl backdrop-blur-lg border border-white/20 shadow-xl`
- Input styling: `bg-black/20 border border-white/10 rounded-xl text-white placeholder-white/40`
- Primary button: `bg-indigo-600 hover:bg-indigo-500 text-white font-medium rounded-xl`
- Error box: `bg-red-500/20 border border-red-500/50 rounded-xl text-red-200`
- Dark background: `bg-zinc-950` with indigo/emerald blur orbs
- Numbered steps: "1. Authenticate Google", "2. Process Canvas Feed", etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API endpoints (/api/auth/me and /api/auth/canvas-url) and auth-aware root page</name>
  <files>src/app/api/auth/me/route.ts, src/app/api/auth/canvas-url/route.ts, src/app/page.tsx, src/app/dashboard/page.tsx</files>
  <action>
    1. Create `src/app/api/auth/me/route.ts` (GET handler):
       - Call `getSession()` — if null, return 401
       - Query `users` table by session.userId to get user record
       - Query `oauthTokens` table for this userId to get connected accounts
       - For each token row, return: `{ role, email (from oauthTokens.email column), connected: true }`
       - **Reconnect detection:** For each connected account, call `getFreshAccessToken(userId, role)`. If it returns null, set `reconnectNeeded: true` for that account. This is how the UI knows to show ReconnectBanner. Note: this adds a small overhead to /api/auth/me but is the simplest path to surface reconnect state to the client.
       - Return JSON: `{ user: { id, name, email, canvasIcsUrl }, accounts: [{ role, email, connected, reconnectNeeded }], setupComplete: boolean }` where setupComplete = personal account connected AND canvasIcsUrl is set

    2. Create `src/app/api/auth/canvas-url/route.ts` (POST handler):
       - Call `getSession()` — if null, return 401
       - Read `{ canvasIcsUrl }` from request body
       - Validate URL format (basic check: starts with http(s)://, contains .ics or is a valid URL)
       - UPDATE users SET canvasIcsUrl = value WHERE id = session.userId
       - Return 200 JSON `{ success: true }`

    3. Rewrite `src/app/page.tsx` as a Server Component:
       - Import `getSession` from `@/lib/session`
       - Import `redirect` from `next/navigation`
       - Call `getSession()`:
         - If session exists: query user + oauthTokens to check setup state
           - If personal account connected AND canvasIcsUrl is set: `redirect("/dashboard")`
           - Otherwise: render SetupWizard with initial props (which step to show, user data)
         - If no session: render SetupWizard at step 1 (unauthenticated state)
       - Pass any error query params (from OAuth callbacks) to SetupWizard as props: `?error=access_denied` or `?error=school_access_denied`
       - Keep the existing background styling (blur orbs, zinc-950 bg)

    4. Create `src/app/dashboard/page.tsx` as a Server Component:
       - Import `getSession` from `@/lib/session`, `redirect` from `next/navigation`
       - Call `getSession()` — if null, `redirect("/")`
       - Query user record for display
       - Render a simple placeholder dashboard: "Welcome, {name}!" message with the glassmorphic card style
       - Show text: "Your accounts are connected. Sync features coming in Phase 2."
       - This is a placeholder — Phase 2 will replace it with the actual sync UI
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/canvas-to-gcal && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>GET /api/auth/me returns user profile + connected accounts + setup status + reconnectNeeded flags per account. POST /api/auth/canvas-url saves Canvas ICS URL. Root page routes to wizard or dashboard based on auth state. Dashboard page exists as placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Build SetupWizard, AccountDropdown, ReconnectBanner components, and wire layout</name>
  <files>src/components/SetupWizard.tsx, src/components/AccountDropdown.tsx, src/components/ReconnectBanner.tsx, src/app/layout.tsx</files>
  <action>
    1. Create `src/components/SetupWizard.tsx` ('use client'):
       - Props: `{ currentStep: number, user?: { name, email, canvasIcsUrl }, accounts?: { role, email, connected }[], error?: string }`
       - Render a 3-step wizard following the existing CalendarSetup numbered-step visual pattern (glassmorphic cards, numbered headings)
       - Progress bar at the top: 3 segments, filled segments for completed + current steps. Use indigo-500 for filled, white/10 for unfilled. Show "Step X of 3" text.

       **Step 1 — Connect Personal Google:**
       - Heading: "1. Connect Your Google Account"
       - Description: "Sign in with your personal Google account to sync your calendars."
       - Primary action: `<a href="/login/google">` styled as the indigo button — text: "Sign in with Google"
       - This step is active when user has no session

       **Step 2 — Link School Google (optional):**
       - Heading: "2. Link School Google Account"
       - Description: "Connect your school Google account to mirror your school calendar. This is optional — you can add it later."
       - Primary action: `<a href="/link/school-google">` styled as indigo button — text: "Link School Account"
       - Secondary action: "Skip for now" button (muted styling, `bg-white/10 text-white/70`) — advances to step 3 by calling a local state setter or navigating
       - If `error === "school_access_denied"`: show error card per auth failure handling decisions:
         - Message: "Your school may restrict third-party app access. Canvas ICS sync still works without your school account."
         - Two buttons: "Try Again" (links to /link/school-google) and "Skip for Now" (advances to step 3)

       **Step 3 — Canvas ICS URL:**
       - Heading: "3. Add Canvas Feed"
       - Description: "Paste your Canvas calendar feed URL to import your assignments."
       - Input field for URL (same styling as existing CalendarSetup input)
       - "Save & Continue" button — calls POST /api/auth/canvas-url, then redirects to /dashboard on success
       - Show error if URL save fails

       General error handling for `error === "access_denied"`:
       - Show at top of wizard: "Something went wrong connecting your account. Please try again."
       - Styled with the existing red error box pattern

    2. Create `src/components/AccountDropdown.tsx` ('use client'):
       - Fetches account status from `/api/auth/me` on mount (useEffect + useState)
       - Renders a button in the nav that shows the user's name or email (truncated)
       - On click, toggles a dropdown panel:
         - For each connected account: show email + role label badge ("Personal" in indigo, "School" in emerald). Use oauthTokens.email for each account — this gives the correct email per account since Plan 01 added email to oauthTokens and Plan 02 stores it from decodeIdToken.
         - If school not connected: show "Link school account" option (links to /link/school-google)
         - Divider line
         - "Sign out" button — calls `fetch("/api/auth/signout", { method: "POST" })` then `window.location.href = "/"`
       - Use glassmorphic styling for dropdown: `bg-zinc-900/90 backdrop-blur-lg border border-white/10 rounded-xl shadow-2xl`
       - Close dropdown on outside click (useRef + useEffect)

    3. Create `src/components/ReconnectBanner.tsx` ('use client'):
       - Props: `{ account: "personal" | "school", onDismiss?: () => void }`
       - Renders a non-blocking banner at the top of the page (within layout flow, not fixed position — placed above {children} in layout)
       - Styling: `bg-amber-500/10 border border-amber-500/30 text-amber-200` — warm warning, not alarming
       - Message varies by account:
         - personal: "Your Google connection has expired."
         - school: "Your school Google connection has expired. Canvas ICS sync still works."
       - "Reconnect" button: links to `/login/google` (personal) or `/link/school-google` (school)
       - Optional dismiss X button (calls onDismiss)

    4. Update `src/app/layout.tsx`:
       - Import `getSession` from `@/lib/session` — check session server-side
       - If session exists: render a top nav bar containing:
         - App title/logo on the left ("Canvas to GCal" or similar)
         - AccountDropdown component on the right
       - If no session: render layout without nav bar (wizard handles its own header)
       - Keep existing font and base styling
       - Update metadata: title to "Canvas to GCal", description to something relevant
       - The nav bar should use: `bg-zinc-900/80 backdrop-blur-lg border-b border-white/10` for consistency with glassmorphic aesthetic
       - **Wire ReconnectBanner:** Create a client wrapper component (e.g., `ReconnectBannerWrapper`) that fetches `/api/auth/me` and, for each account where `reconnectNeeded === true`, renders a `<ReconnectBanner account={role} />`. Place this wrapper between the nav bar and `{children}` in the layout. This ensures the banner appears at the top of every page for authenticated users when token refresh fails. The wrapper should only render when session exists (pass a prop like `isAuthenticated` from the server component layout).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/charl/School/cs_misc/canvas-to-gcal && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>SetupWizard renders 3-step flow with progress bar, OAuth buttons, skip option, and Canvas URL input. AccountDropdown shows connected accounts with email + role label per account (using oauthTokens.email), sign out. ReconnectBanner shows non-blocking reconnect prompt. Layout includes nav bar for authenticated users AND conditionally renders ReconnectBanner when /api/auth/me indicates reconnectNeeded for any account. All TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <what-built>Complete auth flow UI: 3-step setup wizard, account dropdown, reconnect banner wired to layout via /api/auth/me reconnectNeeded flags, auth-aware routing between wizard and dashboard</what-built>
  <how-to-verify>
    1. Ensure environment variables are set (DATABASE_URL, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, SESSION_SECRET, TOKEN_ENCRYPTION_KEY, NEXT_PUBLIC_BASE_URL=http://localhost:3000)
    2. Run migrations: `npx drizzle-kit migrate`
    3. Start dev server: `npm run dev`
    4. Visit http://localhost:3000 — should see setup wizard at Step 1 with "Sign in with Google" button and progress bar showing "Step 1 of 3"
    5. Click "Sign in with Google" — should redirect to Google consent screen
    6. After Google auth, should return to wizard at Step 2 with "Link School Account" and "Skip for now" buttons
    7. Click "Skip for now" — should advance to Step 3 with Canvas URL input
    8. Paste any URL and click "Save & Continue" — should redirect to /dashboard with "Welcome" message
    9. Verify nav bar appears with account dropdown showing your email + "Personal" label
    10. Click dropdown — verify "Link school account" option is visible, "Sign out" button is present
    11. Click "Sign out" — should return to wizard at Step 1
    12. Verify no ReconnectBanner is visible (tokens should be fresh) — banner only appears when getFreshAccessToken returns null
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the flow</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 3 components exist: SetupWizard.tsx, AccountDropdown.tsx, ReconnectBanner.tsx
- Root page routes authenticated+setup-complete users to /dashboard
- Root page shows wizard for unauthenticated or incomplete-setup users
- Dashboard page exists with placeholder content
- /api/auth/me returns user profile, account status, and reconnectNeeded flags
- /api/auth/canvas-url saves Canvas URL to DB
- Layout includes nav bar with AccountDropdown for authenticated users
- Layout renders ReconnectBanner when /api/auth/me indicates reconnectNeeded
- Visual checkpoint confirms the full flow works end-to-end
</verification>

<success_criteria>
- Setup wizard displays 3 steps with progress bar matching existing glassmorphic aesthetic
- Step 1 links to /login/google, Step 2 links to /link/school-google with skip option, Step 3 saves Canvas URL
- School OAuth blocked error shows helpful message with Try Again and Skip buttons (per locked decision)
- Account dropdown shows email + role labels per account (using oauthTokens.email), Link school option when missing, single Sign out action (per locked decisions)
- Returning authenticated users with completed setup bypass wizard and go to dashboard (per locked decision)
- Token refresh failure shows non-blocking amber reconnect banner at top of page via layout.tsx (per locked decision)
- ReconnectBanner is wired: /api/auth/me returns reconnectNeeded, layout renders banner conditionally
- Human verification confirms the complete auth flow works
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-03-SUMMARY.md`
</output>
